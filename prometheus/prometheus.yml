# ===========================================
# TODO: Configure Prometheus scrape targets
# ===========================================

global:
  scrape_interval: 15s

scrape_configs:
  # Example:
  # - job_name: 'go-service'
  #   static_configs:
  #     - targets: ['go-service:8080']
